// ! NOTE: Next.js routing will reference the folder and then the "page.tsx" file INSIDE that folder
// ? http://localhost:3000/performance

export default function PerformancePage() {
  return <div>Performance Page</div>;
}
